const os = require('os');
console.log(os.platform()); // Returns the operating system platform (e.g., 'linux', 'darwin', 'win32')
console.log(os.userInfo()); // Returns the architecture of the operating system (e.g., 'x64', 'arm64')